# [PROJ-XXX] Tên feature

**Story:** [PROJ-XXX](link-to-jira-story)
**Author:** Tên engineer
**Date:** YYYY-MM-DD

---

## Context

[1-2 câu: repo này đảm nhận phần gì trong story. Xem full solution trên Jira story.]

## Changes

- `src/path/file.ts` — mô tả thay đổi
- `src/path/new-file.ts` — (new) mô tả

## Key Decisions

[Những quyết định implementation cụ thể cho repo này mà solution trên Jira không cover.
Ví dụ: chọn dùng bulk write thay vì loop, thêm index XYZ, dùng worker thay vì sync call, ...]

## DB / API / Config Changes (nếu có)

- **DB:** [Collection/table, field mới, index, migration]
- **API:** [Endpoint mới/thay đổi, breaking changes]
- **Config:** [Env vars mới, config files thay đổi]
